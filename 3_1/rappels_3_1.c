#define int SIZE_TAB 100

#include <stdio.h>
#include <stdlib.h> //pour utiliser les macros [return]] EXIT_SUCCESS, dans cette librairie est écrit #define EXIT_SUCCESS 0. Ici 0 est associer à la macro EXIT_SUCCESS

int main (void) {

int tab[SIZE_TAB];

recherche(tab_static);





    return EXIT_SUCCESS;
}



int[] random_tab()
{
    tab_rand[i]=int rand() % 100 ;//L'opérateur % (modulo) limite la plage des résultats possibles à 100 valeurs (0 à 99).
    return tab_rand;
}



void maxrecherche(tab){
int max=0;

for (int i=0,i<100,i++)
    {

    
    if (tab[i]>tab)
        {
            max=i;
        }
    else
    {
        min=i;


    }


    }
    

printf("dans le tableau la valeur maximum est : %d , le min a pour valeur : %d", tab[max],tab[]);



}

void minrecherche(tab){

int min=0;
for (int i=0,i<100,i++)
    {

    
    if (tab[i]<tab[i-1])
        {
            min_indice=i;
            min=tab[i];
        }
    else
    {
        //min=i;


    }

    printf("\n à l'indice %d , et le miminum à l'indice %d", min_indice min);





    }
    

printf("dans le tableau la valeur maximum est : %d , le min a pour valeur : %d", tab[max],tab[]);

printf("\n à l'indice %d , et le miminum à l'indice %d", j, k);

}













